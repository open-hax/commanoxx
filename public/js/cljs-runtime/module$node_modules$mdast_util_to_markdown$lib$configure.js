shadow$provide.module$node_modules$mdast_util_to_markdown$lib$configure = function(require, module, exports) {
  function configure(base, extension) {
    var index = -1;
    let key;
    if (extension.extensions) {
      for (; ++index < extension.extensions.length;) {
        configure(base, extension.extensions[index]);
      }
    }
    for (key in extension) {
      if (own.call(extension, key)) {
        switch(key) {
          case "extensions":
            break;
          case "unsafe":
            index = base[key];
            var right = extension[key];
            right && index.push(...right);
            break;
          case "join":
            index = base[key];
            (right = extension[key]) && index.push(...right);
            break;
          case "handlers":
            index = base[key];
            (right = extension[key]) && Object.assign(index, right);
            break;
          default:
            base.options[key] = extension[key];
        }
      }
    }
    return base;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, configure:{enumerable:!0, get:function() {
    return configure;
  }}});
  const own = {}.hasOwnProperty;
};

//# sourceMappingURL=module$node_modules$mdast_util_to_markdown$lib$configure.js.map
