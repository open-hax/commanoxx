shadow$provide.module$node_modules$zwitch$index = function(require, module, exports) {
  function zwitch(key, options) {
    function one(value, ...parameters) {
      var fn = one.invalid;
      const handlers = one.handlers;
      value && own.call(value, key) && (fn = String(value[key]), fn = own.call(handlers, fn) ? handlers[fn] : one.unknown);
      if (fn) {
        return fn.call(this, value, ...parameters);
      }
    }
    options = options || {};
    one.handlers = options.handlers || {};
    one.invalid = options.invalid;
    one.unknown = options.unknown;
    return one;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, zwitch:{enumerable:!0, get:function() {
    return zwitch;
  }}});
  const own = {}.hasOwnProperty;
};

//# sourceMappingURL=module$node_modules$zwitch$index.js.map
