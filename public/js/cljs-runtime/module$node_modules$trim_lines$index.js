shadow$provide.module$node_modules$trim_lines$index = function(require, module, exports) {
  function trimLines(value) {
    value = String(value);
    const search = /\r?\n|\r/g;
    let match = search.exec(value), last = 0;
    const lines = [];
    for (; match;) {
      lines.push(trimLine(value.slice(last, match.index), last > 0, !0), match[0]), last = match.index + match[0].length, match = search.exec(value);
    }
    lines.push(trimLine(value.slice(last), last > 0, !1));
    return lines.join("");
  }
  function trimLine(value, start, end) {
    let startIndex = 0, endIndex = value.length;
    if (start) {
      for (start = value.codePointAt(startIndex); start === 9 || start === 32;) {
        startIndex++, start = value.codePointAt(startIndex);
      }
    }
    if (end) {
      for (end = value.codePointAt(endIndex - 1); end === 9 || end === 32;) {
        endIndex--, end = value.codePointAt(endIndex - 1);
      }
    }
    return endIndex > startIndex ? value.slice(startIndex, endIndex) : "";
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, trimLines:{enumerable:!0, get:function() {
    return trimLines;
  }}});
};

//# sourceMappingURL=module$node_modules$trim_lines$index.js.map
