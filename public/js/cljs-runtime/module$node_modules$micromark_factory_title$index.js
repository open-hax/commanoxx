shadow$provide.module$node_modules$micromark_factory_title$index = function(require, module, exports) {
  function factoryTitle(effects, ok, nok, type, markerType, stringType) {
    function begin(code) {
      if (code === marker) {
        return effects.enter(markerType), effects.consume(code), effects.exit(markerType), effects.exit(type), ok;
      }
      effects.enter(stringType);
      return atBreak(code);
    }
    function atBreak(code) {
      if (code === marker) {
        return effects.exit(stringType), begin(marker);
      }
      if (code === null) {
        return nok(code);
      }
      if ((0,require$micromark_util_character.markdownLineEnding)(code)) {
        return effects.enter("lineEnding"), effects.consume(code), effects.exit("lineEnding"), (0,require$micromark_factory_space.factorySpace)(effects, atBreak, "linePrefix");
      }
      effects.enter("chunkString", {contentType:"string"});
      return inside(code);
    }
    function inside(code) {
      if (code === marker || code === null || (0,require$micromark_util_character.markdownLineEnding)(code)) {
        return effects.exit("chunkString"), atBreak(code);
      }
      effects.consume(code);
      return code === 92 ? escape : inside;
    }
    function escape(code) {
      return code === marker || code === 92 ? (effects.consume(code), inside) : inside(code);
    }
    let marker;
    return function(code) {
      return code === 34 || code === 39 || code === 40 ? (effects.enter(type), effects.enter(markerType), effects.consume(code), effects.exit(markerType), marker = code === 40 ? 41 : code, begin) : nok(code);
    };
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, factoryTitle:{enumerable:!0, get:function() {
    return factoryTitle;
  }}});
  var require$micromark_factory_space = require("module$node_modules$micromark_factory_space$index"), require$micromark_util_character = require("module$node_modules$micromark_util_character$index");
};

//# sourceMappingURL=module$node_modules$micromark_factory_title$index.js.map
