shadow$provide.module$node_modules$mdast_util_to_markdown$lib$handle$image_reference = function(require, module, exports) {
  function imageReference(node, _, state, info) {
    _ = node.referenceType;
    const exit = state.enter("imageReference");
    let subexit = state.enter("label");
    info = state.createTracker(info);
    let value = info.move("![");
    const alt = state.safe(node.alt, {before:value, after:"]", ...info.current()});
    value += info.move(alt + "][");
    subexit();
    const stack = state.stack;
    state.stack = [];
    subexit = state.enter("reference");
    node = state.safe(state.associationId(node), {before:value, after:"]", ...info.current()});
    subexit();
    state.stack = stack;
    exit();
    return value = _ !== "full" && alt && alt === node ? _ === "shortcut" ? value.slice(0, -1) : value + info.move("]") : value + info.move(node + "]");
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, imageReference:{enumerable:!0, get:function() {
    return imageReference;
  }}});
  imageReference.peek = function() {
    return "!";
  };
};

//# sourceMappingURL=module$node_modules$mdast_util_to_markdown$lib$handle$image_reference.js.map
