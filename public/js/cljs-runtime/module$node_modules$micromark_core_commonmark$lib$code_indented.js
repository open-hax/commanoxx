shadow$provide.module$node_modules$micromark_core_commonmark$lib$code_indented = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, codeIndented:{enumerable:!0, get:function() {
    return codeIndented;
  }}});
  var require$micromark_factory_space = require("module$node_modules$micromark_factory_space$index"), require$micromark_util_character = require("module$node_modules$micromark_util_character$index");
  const codeIndented = {name:"codeIndented", tokenize:function(effects, ok, nok) {
    function afterPrefix(code) {
      const tail = self.events[self.events.length - 1];
      return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], !0).length >= 4 ? atBreak(code) : nok(code);
    }
    function atBreak(code) {
      if (code === null) {
        return after(code);
      }
      if ((0,require$micromark_util_character.markdownLineEnding)(code)) {
        return effects.attempt(furtherStart, atBreak, after)(code);
      }
      effects.enter("codeFlowValue");
      return inside(code);
    }
    function inside(code) {
      if (code === null || (0,require$micromark_util_character.markdownLineEnding)(code)) {
        return effects.exit("codeFlowValue"), atBreak(code);
      }
      effects.consume(code);
      return inside;
    }
    function after(code) {
      effects.exit("codeIndented");
      return ok(code);
    }
    const self = this;
    return function(code) {
      effects.enter("codeIndented");
      return (0,require$micromark_factory_space.factorySpace)(effects, afterPrefix, "linePrefix", 5)(code);
    };
  }}, furtherStart = {partial:!0, tokenize:function(effects, ok, nok) {
    function furtherStart(code) {
      return self.parser.lazy[self.now().line] ? nok(code) : (0,require$micromark_util_character.markdownLineEnding)(code) ? (effects.enter("lineEnding"), effects.consume(code), effects.exit("lineEnding"), furtherStart) : (0,require$micromark_factory_space.factorySpace)(effects, afterPrefix, "linePrefix", 5)(code);
    }
    function afterPrefix(code) {
      const tail = self.events[self.events.length - 1];
      return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], !0).length >= 4 ? ok(code) : (0,require$micromark_util_character.markdownLineEnding)(code) ? furtherStart(code) : nok(code);
    }
    const self = this;
    return furtherStart;
  }};
};

//# sourceMappingURL=module$node_modules$micromark_core_commonmark$lib$code_indented.js.map
