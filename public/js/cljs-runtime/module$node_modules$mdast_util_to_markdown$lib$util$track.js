shadow$provide.module$node_modules$mdast_util_to_markdown$lib$util$track = function(require, module, exports) {
  function track(config) {
    config = config || {};
    const now = config.now || {};
    let lineShift = config.lineShift || 0, line = now.line || 1, column = now.column || 1;
    return {move:function(input) {
      input = input || "";
      const chunks = input.split(/\r?\n|\r/g), tail = chunks[chunks.length - 1];
      line += chunks.length - 1;
      column = chunks.length === 1 ? column + tail.length : 1 + tail.length + lineShift;
      return input;
    }, current:function() {
      return {now:{line, column}, lineShift};
    }, shift:function(value) {
      lineShift += value;
    }};
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, track:{enumerable:!0, get:function() {
    return track;
  }}});
};

//# sourceMappingURL=module$node_modules$mdast_util_to_markdown$lib$util$track.js.map
