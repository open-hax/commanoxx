shadow$provide.module$node_modules$mdast_util_to_hast$lib$revert = function(require, module, exports) {
  function revert(state, node) {
    const subtype = node.referenceType;
    let suffix = "]";
    subtype === "collapsed" ? suffix += "[]" : subtype === "full" && (suffix += "[" + (node.label || node.identifier) + "]");
    if (node.type === "imageReference") {
      return [{type:"text", value:"![" + node.alt + suffix}];
    }
    state = state.all(node);
    (node = state[0]) && node.type === "text" ? node.value = "[" + node.value : state.unshift({type:"text", value:"["});
    (node = state[state.length - 1]) && node.type === "text" ? node.value += suffix : state.push({type:"text", value:suffix});
    return state;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, revert:{enumerable:!0, get:function() {
    return revert;
  }}});
};

//# sourceMappingURL=module$node_modules$mdast_util_to_hast$lib$revert.js.map
