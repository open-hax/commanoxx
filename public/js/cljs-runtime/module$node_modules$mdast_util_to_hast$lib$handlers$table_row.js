shadow$provide.module$node_modules$mdast_util_to_hast$lib$handlers$table_row = function(require, module, exports) {
  function tableRow(state, node, parent) {
    var siblings = parent ? parent.children : void 0;
    siblings = (siblings ? siblings.indexOf(node) : 1) === 0 ? "th" : "td";
    const length = (parent = parent && parent.type === "table" ? parent.align : void 0) ? parent.length : node.children.length;
    let cellIndex = -1;
    const cells = [];
    for (; ++cellIndex < length;) {
      const cell = node.children[cellIndex];
      var properties = {};
      const alignValue = parent ? parent[cellIndex] : void 0;
      alignValue && (properties.align = alignValue);
      properties = {type:"element", tagName:siblings, properties, children:[]};
      cell && (properties.children = state.all(cell), state.patch(cell, properties), properties = state.applyData(cell, properties));
      cells.push(properties);
    }
    siblings = {type:"element", tagName:"tr", properties:{}, children:state.wrap(cells, !0)};
    state.patch(node, siblings);
    return state.applyData(node, siblings);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, tableRow:{enumerable:!0, get:function() {
    return tableRow;
  }}});
};

//# sourceMappingURL=module$node_modules$mdast_util_to_hast$lib$handlers$table_row.js.map
