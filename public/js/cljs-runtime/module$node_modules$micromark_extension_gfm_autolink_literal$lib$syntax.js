shadow$provide.module$node_modules$micromark_extension_gfm_autolink_literal$lib$syntax = function(require, module, exports) {
  function gfmAutolinkLiteral() {
    return {text};
  }
  function previousWww(code) {
    return code === null || code === 40 || code === 42 || code === 95 || code === 91 || code === 93 || code === 126 || (0,require$micromark_util_character.markdownLineEndingOrSpace)(code);
  }
  function previousProtocol(code) {
    return !(0,require$micromark_util_character.asciiAlpha)(code);
  }
  function previousEmail(code) {
    return !(code === 47 || gfmAtext(code));
  }
  function gfmAtext(code) {
    return code === 43 || code === 45 || code === 46 || code === 95 || (0,require$micromark_util_character.asciiAlphanumeric)(code);
  }
  function previousUnbalanced(events) {
    let index = events.length, result = !1;
    for (; index--;) {
      const token = events[index][1];
      if ((token.type === "labelLink" || token.type === "labelImage") && !token._balanced) {
        result = !0;
        break;
      }
      if (token._gfmAutolinkLiteralWalkedInto) {
        result = !1;
        break;
      }
    }
    events.length > 0 && !result && (events[events.length - 1][1]._gfmAutolinkLiteralWalkedInto = !0);
    return result;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, gfmAutolinkLiteral:{enumerable:!0, get:function() {
    return gfmAutolinkLiteral;
  }}});
  var require$micromark_util_character = require("module$node_modules$micromark_util_character$index");
  const wwwPrefix = {tokenize:function(effects, ok, nok) {
    function wwwPrefixInside(code) {
      return (code === 87 || code === 119) && size < 3 ? (size++, effects.consume(code), wwwPrefixInside) : code === 46 && size === 3 ? (effects.consume(code), wwwPrefixAfter) : nok(code);
    }
    function wwwPrefixAfter(code) {
      return code === null ? nok(code) : ok(code);
    }
    let size = 0;
    return wwwPrefixInside;
  }, partial:!0}, domain = {tokenize:function(effects, ok, nok) {
    function domainInside(code) {
      if (code === 46 || code === 95) {
        return effects.check(trail, domainAfter, domainAtPunctuation)(code);
      }
      if (code === null || (0,require$micromark_util_character.markdownLineEndingOrSpace)(code) || (0,require$micromark_util_character.unicodeWhitespace)(code) || code !== 45 && (0,require$micromark_util_character.unicodePunctuation)(code)) {
        return domainAfter(code);
      }
      seen = !0;
      effects.consume(code);
      return domainInside;
    }
    function domainAtPunctuation(code) {
      code === 95 ? underscoreInLastSegment = !0 : (underscoreInLastLastSegment = underscoreInLastSegment, underscoreInLastSegment = void 0);
      effects.consume(code);
      return domainInside;
    }
    function domainAfter(code) {
      return underscoreInLastLastSegment || underscoreInLastSegment || !seen ? nok(code) : ok(code);
    }
    let underscoreInLastSegment, underscoreInLastLastSegment, seen;
    return domainInside;
  }, partial:!0}, path = {tokenize:function(effects, ok) {
    function pathInside(code) {
      if (code === 40) {
        return sizeOpen++, effects.consume(code), pathInside;
      }
      if (code === 41 && sizeClose < sizeOpen) {
        return pathAtPunctuation(code);
      }
      if (code === 33 || code === 34 || code === 38 || code === 39 || code === 41 || code === 42 || code === 44 || code === 46 || code === 58 || code === 59 || code === 60 || code === 63 || code === 93 || code === 95 || code === 126) {
        return effects.check(trail, ok, pathAtPunctuation)(code);
      }
      if (code === null || (0,require$micromark_util_character.markdownLineEndingOrSpace)(code) || (0,require$micromark_util_character.unicodeWhitespace)(code)) {
        return ok(code);
      }
      effects.consume(code);
      return pathInside;
    }
    function pathAtPunctuation(code) {
      code === 41 && sizeClose++;
      effects.consume(code);
      return pathInside;
    }
    let sizeOpen = 0, sizeClose = 0;
    return pathInside;
  }, partial:!0}, trail = {tokenize:function(effects, ok, nok) {
    function trail(code) {
      return code === 33 || code === 34 || code === 39 || code === 41 || code === 42 || code === 44 || code === 46 || code === 58 || code === 59 || code === 63 || code === 95 || code === 126 ? (effects.consume(code), trail) : code === 38 ? (effects.consume(code), trailCharacterReferenceStart) : code === 93 ? (effects.consume(code), trailBracketAfter) : code === 60 || code === null || (0,require$micromark_util_character.markdownLineEndingOrSpace)(code) || (0,require$micromark_util_character.unicodeWhitespace)(code) ? 
      ok(code) : nok(code);
    }
    function trailBracketAfter(code) {
      return code === null || code === 40 || code === 91 || (0,require$micromark_util_character.markdownLineEndingOrSpace)(code) || (0,require$micromark_util_character.unicodeWhitespace)(code) ? ok(code) : trail(code);
    }
    function trailCharacterReferenceStart(code) {
      return (0,require$micromark_util_character.asciiAlpha)(code) ? trailCharacterReferenceInside(code) : nok(code);
    }
    function trailCharacterReferenceInside(code) {
      return code === 59 ? (effects.consume(code), trail) : (0,require$micromark_util_character.asciiAlpha)(code) ? (effects.consume(code), trailCharacterReferenceInside) : nok(code);
    }
    return trail;
  }, partial:!0}, emailDomainDotTrail = {tokenize:function(effects, ok, nok) {
    function after(code) {
      return (0,require$micromark_util_character.asciiAlphanumeric)(code) ? nok(code) : ok(code);
    }
    return function(code) {
      effects.consume(code);
      return after;
    };
  }, partial:!0};
  require = {name:"wwwAutolink", tokenize:function(effects, ok, nok) {
    function wwwAfter(code) {
      effects.exit("literalAutolinkWww");
      effects.exit("literalAutolink");
      return ok(code);
    }
    const self = this;
    return function(code) {
      if (code !== 87 && code !== 119 || !previousWww.call(self, self.previous) || previousUnbalanced(self.events)) {
        return nok(code);
      }
      effects.enter("literalAutolink");
      effects.enter("literalAutolinkWww");
      return effects.check(wwwPrefix, effects.attempt(domain, effects.attempt(path, wwwAfter), nok), nok)(code);
    };
  }, previous:previousWww};
  module = {name:"protocolAutolink", tokenize:function(effects, ok, nok) {
    function protocolPrefixInside(code) {
      if ((0,require$micromark_util_character.asciiAlpha)(code) && buffer.length < 5) {
        return buffer += String.fromCodePoint(code), effects.consume(code), protocolPrefixInside;
      }
      if (code === 58) {
        const protocol = buffer.toLowerCase();
        if (protocol === "http" || protocol === "https") {
          return effects.consume(code), protocolSlashesInside;
        }
      }
      return nok(code);
    }
    function protocolSlashesInside(code) {
      if (code === 47) {
        effects.consume(code);
        if (seen) {
          return afterProtocol;
        }
        seen = !0;
        return protocolSlashesInside;
      }
      return nok(code);
    }
    function afterProtocol(code) {
      return code === null || (0,require$micromark_util_character.asciiControl)(code) || (0,require$micromark_util_character.markdownLineEndingOrSpace)(code) || (0,require$micromark_util_character.unicodeWhitespace)(code) || (0,require$micromark_util_character.unicodePunctuation)(code) ? nok(code) : effects.attempt(domain, effects.attempt(path, protocolAfter), nok)(code);
    }
    function protocolAfter(code) {
      effects.exit("literalAutolinkHttp");
      effects.exit("literalAutolink");
      return ok(code);
    }
    const self = this;
    let buffer = "", seen = !1;
    return function(code) {
      return code !== 72 && code !== 104 || !previousProtocol.call(self, self.previous) || previousUnbalanced(self.events) ? nok(code) : (effects.enter("literalAutolink"), effects.enter("literalAutolinkHttp"), buffer += String.fromCodePoint(code), effects.consume(code), protocolPrefixInside);
    };
  }, previous:previousProtocol};
  exports = {name:"emailAutolink", tokenize:function(effects, ok, nok) {
    function atext(code) {
      return gfmAtext(code) ? (effects.consume(code), atext) : code === 64 ? (effects.consume(code), emailDomain) : nok(code);
    }
    function emailDomain(code) {
      return code === 46 ? effects.check(emailDomainDotTrail, emailDomainAfter, emailDomainDot)(code) : code === 45 || code === 95 || (0,require$micromark_util_character.asciiAlphanumeric)(code) ? (data = !0, effects.consume(code), emailDomain) : emailDomainAfter(code);
    }
    function emailDomainDot(code) {
      effects.consume(code);
      dot = !0;
      return emailDomain;
    }
    function emailDomainAfter(code) {
      return data && dot && (0,require$micromark_util_character.asciiAlpha)(self.previous) ? (effects.exit("literalAutolinkEmail"), effects.exit("literalAutolink"), ok(code)) : nok(code);
    }
    const self = this;
    let dot, data;
    return function(code) {
      if (!gfmAtext(code) || !previousEmail.call(self, self.previous) || previousUnbalanced(self.events)) {
        return nok(code);
      }
      effects.enter("literalAutolink");
      effects.enter("literalAutolinkEmail");
      return atext(code);
    };
  }, previous:previousEmail};
  const text = {};
  let code = 48;
  for (; code < 123;) {
    text[code] = exports, code++, code === 58 ? code = 65 : code === 91 && (code = 97);
  }
  text[43] = exports;
  text[45] = exports;
  text[46] = exports;
  text[95] = exports;
  text[72] = [exports, module];
  text[104] = [exports, module];
  text[87] = [exports, require];
  text[119] = [exports, require];
};

//# sourceMappingURL=module$node_modules$micromark_extension_gfm_autolink_literal$lib$syntax.js.map
