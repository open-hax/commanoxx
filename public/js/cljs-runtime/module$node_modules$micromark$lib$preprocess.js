shadow$provide.module$node_modules$micromark$lib$preprocess = function(require, module, exports) {
  function preprocess() {
    let column = 1, buffer = "", start = !0, atCarriageReturn;
    return function(value, encoding, end) {
      const chunks = [];
      var match;
      let startPosition, code;
      value = buffer + (typeof value === "string" ? value.toString() : (new TextDecoder(encoding || void 0)).decode(value));
      startPosition = 0;
      buffer = "";
      start && (value.charCodeAt(0) === 65279 && startPosition++, start = void 0);
      for (; startPosition < value.length;) {
        search.lastIndex = startPosition;
        encoding = (match = search.exec(value)) && match.index !== void 0 ? match.index : value.length;
        code = value.charCodeAt(encoding);
        if (!match) {
          buffer = value.slice(startPosition);
          break;
        }
        if (code === 10 && startPosition === encoding && atCarriageReturn) {
          chunks.push(-3), atCarriageReturn = void 0;
        } else {
          switch(atCarriageReturn && (chunks.push(-5), atCarriageReturn = void 0), startPosition < encoding && (chunks.push(value.slice(startPosition, encoding)), column += encoding - startPosition), code) {
            case 0:
              chunks.push(65533);
              column++;
              break;
            case 9:
              match = Math.ceil(column / 4) * 4;
              for (chunks.push(-2); column++ < match;) {
                chunks.push(-1);
              }
              break;
            case 10:
              chunks.push(-4);
              column = 1;
              break;
            default:
              atCarriageReturn = !0, column = 1;
          }
        }
        startPosition = encoding + 1;
      }
      end && (atCarriageReturn && chunks.push(-5), buffer && chunks.push(buffer), chunks.push(null));
      return chunks;
    };
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, preprocess:{enumerable:!0, get:function() {
    return preprocess;
  }}});
  const search = /[\0\t\n\r]/g;
};

//# sourceMappingURL=module$node_modules$micromark$lib$preprocess.js.map
