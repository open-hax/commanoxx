shadow$provide.module$node_modules$micromark_extension_gfm_table$lib$edit_map = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, EditMap:{enumerable:!0, get:function() {
    return EditMap;
  }}});
  class EditMap {
    constructor() {
      this.map = [];
    }
    add(index$jscomp$0, remove, add) {
      a: {
        let index = 0;
        if (remove !== 0 || add.length !== 0) {
          for (; index < this.map.length;) {
            if (this.map[index][0] === index$jscomp$0) {
              this.map[index][1] += remove;
              this.map[index][2].push(...add);
              break a;
            }
            index += 1;
          }
          this.map.push([index$jscomp$0, remove, add]);
        }
      }
    }
    consume(events) {
      this.map.sort(function(a, b) {
        return a[0] - b[0];
      });
      if (this.map.length !== 0) {
        for (var index = this.map.length, vecs = []; index > 0;) {
          --index, vecs.push(events.slice(this.map[index][0] + this.map[index][1]), this.map[index][2]), events.length = this.map[index][0];
        }
        vecs.push(events.slice());
        events.length = 0;
        for (index = vecs.pop(); index;) {
          for (const element of index) {
            events.push(element);
          }
          index = vecs.pop();
        }
        this.map.length = 0;
      }
    }
  }
};

//# sourceMappingURL=module$node_modules$micromark_extension_gfm_table$lib$edit_map.js.map
