shadow$provide.module$node_modules$mdast_util_to_markdown$lib$handle$link_reference = function(require, module, exports) {
  function linkReference(node, _, state, info) {
    _ = node.referenceType;
    const exit = state.enter("linkReference");
    let subexit = state.enter("label");
    info = state.createTracker(info);
    let value = info.move("[");
    const text = state.containerPhrasing(node, {before:value, after:"]", ...info.current()});
    value += info.move(text + "][");
    subexit();
    const stack = state.stack;
    state.stack = [];
    subexit = state.enter("reference");
    node = state.safe(state.associationId(node), {before:value, after:"]", ...info.current()});
    subexit();
    state.stack = stack;
    exit();
    return value = _ !== "full" && text && text === node ? _ === "shortcut" ? value.slice(0, -1) : value + info.move("]") : value + info.move(node + "]");
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, linkReference:{enumerable:!0, get:function() {
    return linkReference;
  }}});
  linkReference.peek = function() {
    return "[";
  };
};

//# sourceMappingURL=module$node_modules$mdast_util_to_markdown$lib$handle$link_reference.js.map
