shadow$provide.module$node_modules$hast_util_parse_selector$lib$index = function(require, module, exports) {
  function parseSelector(selector, defaultTagName) {
    selector = selector || "";
    const props = {};
    let start = 0, previous, tagName;
    for (; start < selector.length;) {
      search.lastIndex = start;
      const match = search.exec(selector), subvalue = selector.slice(start, match ? match.index : selector.length);
      subvalue && (previous ? previous === "#" ? props.id = subvalue : Array.isArray(props.className) ? props.className.push(subvalue) : props.className = [subvalue] : tagName = subvalue, start += subvalue.length);
      match && (previous = match[0], start++);
    }
    return {type:"element", tagName:tagName || defaultTagName || "div", properties:props, children:[]};
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, parseSelector:{enumerable:!0, get:function() {
    return parseSelector;
  }}});
  const search = /[#.]/g;
};

//# sourceMappingURL=module$node_modules$hast_util_parse_selector$lib$index.js.map
