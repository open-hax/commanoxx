shadow$provide.module$node_modules$mdast_util_to_markdown$lib$handle$inline_code = function(require, module, exports) {
  function inlineCode(node, _, state) {
    node = node.value || "";
    _ = "`";
    let index = -1;
    for (; (new RegExp("(^|[^`])" + _ + "([^`]|$)")).test(node);) {
      _ += "`";
    }
    for (/[^ \r\n]/.test(node) && (/^[ \r\n]/.test(node) && /[ \r\n]$/.test(node) || /^`|`$/.test(node)) && (node = " " + node + " "); ++index < state.unsafe.length;) {
      var pattern = state.unsafe[index];
      const expression = state.compilePattern(pattern);
      if (pattern.atBreak) {
        for (; pattern = expression.exec(node);) {
          let position = pattern.index;
          node.charCodeAt(position) === 10 && node.charCodeAt(position - 1) === 13 && position--;
          node = node.slice(0, position) + " " + node.slice(pattern.index + 1);
        }
      }
    }
    return _ + node + _;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, inlineCode:{enumerable:!0, get:function() {
    return inlineCode;
  }}});
  inlineCode.peek = function() {
    return "`";
  };
};

//# sourceMappingURL=module$node_modules$mdast_util_to_markdown$lib$handle$inline_code.js.map
