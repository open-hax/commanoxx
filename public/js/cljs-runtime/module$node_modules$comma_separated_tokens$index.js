shadow$provide.module$node_modules$comma_separated_tokens$index = function(require, module, exports) {
  function parse(value) {
    const tokens = [];
    value = String(value || "");
    let index = value.indexOf(",");
    var start = 0;
    let end = !1;
    for (; !end;) {
      index === -1 && (index = value.length, end = !0), start = value.slice(start, index).trim(), !start && end || tokens.push(start), start = index + 1, index = value.indexOf(",", start);
    }
    return tokens;
  }
  function stringify(values, options) {
    options = options || {};
    return (values[values.length - 1] === "" ? [...values, ""] : values).join((options.padRight ? " " : "") + "," + (options.padLeft === !1 ? "" : " ")).trim();
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, parse:{enumerable:!0, get:function() {
    return parse;
  }}, stringify:{enumerable:!0, get:function() {
    return stringify;
  }}});
};

//# sourceMappingURL=module$node_modules$comma_separated_tokens$index.js.map
