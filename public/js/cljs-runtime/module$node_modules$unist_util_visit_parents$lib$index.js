shadow$provide.module$node_modules$unist_util_visit_parents$lib$index = function(require, module, exports) {
  function visitParents(tree, test, visitor, reverse) {
    function factory(node, index, parents) {
      function visit() {
        var result = empty;
        let subresult, offset, grandparents;
        if (!test || is(node, index, parents[parents.length - 1] || void 0)) {
          if (result = visitor(node, parents), result = Array.isArray(result) ? result : typeof result === "number" ? [!0, result] : result === null || result === void 0 ? empty : [result], result[0] === !1) {
            return result;
          }
        }
        if ("children" in node && node.children && node.children && result[0] !== "skip") {
          for (offset = (reverse ? node.children.length : -1) + step, grandparents = parents.concat(node); offset > -1 && offset < node.children.length;) {
            subresult = factory(node.children[offset], offset, grandparents)();
            if (subresult[0] === !1) {
              return subresult;
            }
            offset = typeof subresult[1] === "number" ? subresult[1] : offset + step;
          }
        }
        return result;
      }
      var value = node && typeof node === "object" ? node : {};
      typeof value.type === "string" && (value = typeof value.tagName === "string" ? value.tagName : typeof value.name === "string" ? value.name : void 0, Object.defineProperty(visit, "name", {value:"node (" + (0,require$unist_util_visit_parents_SLASH_do_not_use_color.color)(node.type + (value ? "\x3c" + value + "\x3e" : "")) + ")"}));
      return visit;
    }
    let check;
    typeof test === "function" && typeof visitor !== "function" ? (reverse = visitor, visitor = test) : check = test;
    const is = (0,require$unist_util_is.convert)(check), step = reverse ? -1 : 1;
    factory(tree, void 0, [])();
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, CONTINUE:{enumerable:!0, get:function() {
    return !0;
  }}, EXIT:{enumerable:!0, get:function() {
    return !1;
  }}, SKIP:{enumerable:!0, get:function() {
    return "skip";
  }}, visitParents:{enumerable:!0, get:function() {
    return visitParents;
  }}});
  var require$unist_util_is = require("module$node_modules$unist_util_is$index"), require$unist_util_visit_parents_SLASH_do_not_use_color = require("module$node_modules$unist_util_visit_parents$lib$color");
  const empty = [];
};

//# sourceMappingURL=module$node_modules$unist_util_visit_parents$lib$index.js.map
