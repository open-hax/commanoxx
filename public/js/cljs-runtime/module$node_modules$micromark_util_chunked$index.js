shadow$provide.module$node_modules$micromark_util_chunked$index = function(require, module, exports) {
  function splice(list, start, remove, items) {
    var end = list.length;
    let chunkStart = 0;
    start = start < 0 ? -start > end ? 0 : end + start : start > end ? end : start;
    remove = remove > 0 ? remove : 0;
    if (items.length < 10000) {
      end = Array.from(items), end.unshift(start, remove), list.splice(...end);
    } else {
      for (remove && list.splice(start, remove); chunkStart < items.length;) {
        end = items.slice(chunkStart, chunkStart + 10000), end.unshift(start, 0), list.splice(...end), chunkStart += 10000, start += 10000;
      }
    }
  }
  function push(list, items) {
    return list.length > 0 ? (splice(list, list.length, 0, items), list) : items;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, push:{enumerable:!0, get:function() {
    return push;
  }}, splice:{enumerable:!0, get:function() {
    return splice;
  }}});
};

//# sourceMappingURL=module$node_modules$micromark_util_chunked$index.js.map
