shadow$provide.module$node_modules$micromark_util_combine_extensions$index = function(require, module, exports) {
  function combineExtensions(extensions) {
    const all = {};
    let index = -1;
    for (; ++index < extensions.length;) {
      let hook = void 0;
      var all$jscomp$0 = all, extension = extensions[index];
      for (hook in extension) {
        const left = (hasOwnProperty.call(all$jscomp$0, hook) ? all$jscomp$0[hook] : void 0) || (all$jscomp$0[hook] = {}), right = extension[hook];
        let code;
        if (right) {
          for (code in right) {
            hasOwnProperty.call(left, code) || (left[code] = []);
            var value = right[code], existing = left[code];
            value = Array.isArray(value) ? value : value ? [value] : [];
            let index = -1;
            const before = [];
            for (; ++index < value.length;) {
              (value[index].add === "after" ? existing : before).push(value[index]);
            }
            (0,require$micromark_util_chunked.splice)(existing, 0, 0, before);
          }
        }
      }
    }
    return all;
  }
  function combineHtmlExtensions(htmlExtensions) {
    const handlers = {};
    let index = -1;
    for (; ++index < htmlExtensions.length;) {
      let hook = void 0;
      var all = handlers, extension = htmlExtensions[index];
      for (hook in extension) {
        const left = (hasOwnProperty.call(all, hook) ? all[hook] : void 0) || (all[hook] = {}), right = extension[hook];
        let type;
        if (right) {
          for (type in right) {
            left[type] = right[type];
          }
        }
      }
    }
    return handlers;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, combineExtensions:{enumerable:!0, get:function() {
    return combineExtensions;
  }}, combineHtmlExtensions:{enumerable:!0, get:function() {
    return combineHtmlExtensions;
  }}});
  var require$micromark_util_chunked = require("module$node_modules$micromark_util_chunked$index");
  const hasOwnProperty = {}.hasOwnProperty;
};

//# sourceMappingURL=module$node_modules$micromark_util_combine_extensions$index.js.map
