shadow$provide.module$node_modules$mdast_util_to_string$lib$index = function(require, module, exports) {
  function toString(value, options) {
    options = options || emptyOptions;
    return one(value, typeof options.includeImageAlt === "boolean" ? options.includeImageAlt : !0, typeof options.includeHtml === "boolean" ? options.includeHtml : !0);
  }
  function one(value, includeImageAlt, includeHtml) {
    if (value && typeof value === "object") {
      if ("value" in value) {
        return value.type !== "html" || includeHtml ? value.value : "";
      }
      if (includeImageAlt && "alt" in value && value.alt) {
        return value.alt;
      }
      if ("children" in value) {
        return all(value.children, includeImageAlt, includeHtml);
      }
    }
    return Array.isArray(value) ? all(value, includeImageAlt, includeHtml) : "";
  }
  function all(values, includeImageAlt, includeHtml) {
    const result = [];
    let index = -1;
    for (; ++index < values.length;) {
      result[index] = one(values[index], includeImageAlt, includeHtml);
    }
    return result.join("");
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, toString:{enumerable:!0, get:function() {
    return toString;
  }}});
  const emptyOptions = {};
};

//# sourceMappingURL=module$node_modules$mdast_util_to_string$lib$index.js.map
