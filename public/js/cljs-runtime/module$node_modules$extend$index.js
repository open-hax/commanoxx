shadow$provide.module$node_modules$extend$index = function(require, module, exports) {
  var hasOwn = Object.prototype.hasOwnProperty, toStr = Object.prototype.toString, defineProperty = Object.defineProperty, gOPD = Object.getOwnPropertyDescriptor, isArray = function(arr) {
    return typeof Array.isArray === "function" ? Array.isArray(arr) : toStr.call(arr) === "[object Array]";
  }, isPlainObject = function(obj) {
    if (!obj || toStr.call(obj) !== "[object Object]") {
      return !1;
    }
    var hasOwnConstructor = hasOwn.call(obj, "constructor"), hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, "isPrototypeOf");
    if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
      return !1;
    }
    for (var key in obj) {
    }
    return typeof key === "undefined" || hasOwn.call(obj, key);
  }, setProperty = function(target, options) {
    defineProperty && options.name === "__proto__" ? defineProperty(target, options.name, {enumerable:!0, configurable:!0, value:options.newValue, writable:!0}) : target[options.name] = options.newValue;
  }, getProperty = function(obj, name) {
    if (name === "__proto__") {
      if (!hasOwn.call(obj, name)) {
        return;
      }
      if (gOPD) {
        return gOPD(obj, name).value;
      }
    }
    return obj[name];
  };
  module.exports = function extend() {
    var name, copyIsArray, target = arguments[0], i = 1, length = arguments.length, deep = !1;
    typeof target === "boolean" && (deep = target, target = arguments[1] || {}, i = 2);
    if (target == null || typeof target !== "object" && typeof target !== "function") {
      target = {};
    }
    for (; i < length; ++i) {
      var options = arguments[i];
      if (options != null) {
        for (name in options) {
          var src = getProperty(target, name);
          var copy = getProperty(options, name);
          target !== copy && (deep && copy && (isPlainObject(copy) || (copyIsArray = isArray(copy))) ? (copyIsArray ? (copyIsArray = !1, src = src && isArray(src) ? src : []) : src = src && isPlainObject(src) ? src : {}, setProperty(target, {name, newValue:extend(deep, src, copy)})) : typeof copy !== "undefined" && setProperty(target, {name, newValue:copy}));
        }
      }
    }
    return target;
  };
};

//# sourceMappingURL=module$node_modules$extend$index.js.map
