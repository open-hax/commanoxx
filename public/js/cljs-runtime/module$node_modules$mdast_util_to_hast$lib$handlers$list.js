shadow$provide.module$node_modules$mdast_util_to_hast$lib$handlers$list = function(require, module, exports) {
  function list(state, node) {
    var properties = {};
    const results = state.all(node);
    let index = -1;
    typeof node.start === "number" && node.start !== 1 && (properties.start = node.start);
    for (; ++index < results.length;) {
      const child = results[index];
      if (child.type === "element" && child.tagName === "li" && child.properties && Array.isArray(child.properties.className) && child.properties.className.includes("task-list-item")) {
        properties.className = ["contains-task-list"];
        break;
      }
    }
    properties = {type:"element", tagName:node.ordered ? "ol" : "ul", properties, children:state.wrap(results, !0)};
    state.patch(node, properties);
    return state.applyData(node, properties);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, list:{enumerable:!0, get:function() {
    return list;
  }}});
};

//# sourceMappingURL=module$node_modules$mdast_util_to_hast$lib$handlers$list.js.map
